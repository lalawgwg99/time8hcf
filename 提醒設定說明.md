# 提醒設定說明

## 📢 目前的提醒設定

### 震動效果
- **次數**: 5次
- **模式**: 震-停-震-停-震-停-震-停-震
- **時長**: 每次震動 400ms,間隔 200ms

### 聲音效果
- **次數**: 3次 (每次播放 C-E-G 三個音符)
- **間隔**: 每次提醒音間隔 0.8 秒
- **總時長**: 約 2 秒

---

## 🔧 如何自訂提醒次數

### 修改震動次數

在 `index.html` 第 665 行找到:

```javascript
navigator.vibrate([400, 200, 400, 200, 400, 200, 400, 200, 400]);
```

**說明**:
- 奇數位置的數字 (400) = 震動時長 (毫秒)
- 偶數位置的數字 (200) = 停止時長 (毫秒)

**範例**:

**2次震動**:
```javascript
navigator.vibrate([400, 200, 400]);
```

**3次震動**:
```javascript
navigator.vibrate([400, 200, 400, 200, 400]);
```

**10次震動**:
```javascript
navigator.vibrate([400, 200, 400, 200, 400, 200, 400, 200, 400, 200, 400, 200, 400, 200, 400, 200, 400, 200, 400]);
```

**超長震動 (持續2秒)**:
```javascript
navigator.vibrate([2000]);
```

---

### 修改聲音次數

在 `index.html` 第 678-694 行找到提醒音的代碼。

**目前是 3 次提醒音**:
```javascript
// 第1次提醒音
synth.triggerAttackRelease("C5", noteDuration, now);
synth.triggerAttackRelease("E5", noteDuration, now + interval);
synth.triggerAttackRelease("G5", noteDuration, now + (2 * interval));

// 第2次提醒音 (延遲0.8秒)
synth.triggerAttackRelease("C5", noteDuration, now + 0.8);
synth.triggerAttackRelease("E5", noteDuration, now + 0.8 + interval);
synth.triggerAttackRelease("G5", noteDuration, now + 0.8 + (2 * interval));

// 第3次提醒音 (延遲1.6秒)
synth.triggerAttackRelease("C5", noteDuration, now + 1.6);
synth.triggerAttackRelease("E5", noteDuration, now + 1.6 + interval);
synth.triggerAttackRelease("G5", noteDuration, now + 1.6 + (2 * interval));
```

**如果只要 1 次提醒音**:
刪除第2次和第3次的代碼,只保留:
```javascript
// 第1次提醒音
synth.triggerAttackRelease("C5", noteDuration, now);
synth.triggerAttackRelease("E5", noteDuration, now + interval);
synth.triggerAttackRelease("G5", noteDuration, now + (2 * interval));
```

**如果要 5 次提醒音**:
新增第4次和第5次:
```javascript
// ... 保留前3次 ...

// 第4次提醒音 (延遲2.4秒)
synth.triggerAttackRelease("C5", noteDuration, now + 2.4);
synth.triggerAttackRelease("E5", noteDuration, now + 2.4 + interval);
synth.triggerAttackRelease("G5", noteDuration, now + 2.4 + (2 * interval));

// 第5次提醒音 (延遲3.2秒)
synth.triggerAttackRelease("C5", noteDuration, now + 3.2);
synth.triggerAttackRelease("E5", noteDuration, now + 3.2 + interval);
synth.triggerAttackRelease("G5", noteDuration, now + 3.2 + (2 * interval));
```

**延遲時間計算規則**:
- 第1次: `now` (立即)
- 第2次: `now + 0.8`
- 第3次: `now + 1.6` (0.8 × 2)
- 第4次: `now + 2.4` (0.8 × 3)
- 第5次: `now + 3.2` (0.8 × 4)
- 以此類推...

---

## 🎵 修改提醒音調

如果想改變提醒的音調,可以修改音符:

**目前音符**: C5, E5, G5 (C大調和弦)

**其他選項**:
- **更高音**: C6, E6, G6
- **更低音**: C4, E4, G4
- **警報音**: A5, A5, A5 (重複同音)
- **下降音**: G5, E5, C5 (由高到低)

**範例 - 改為警報音**:
```javascript
synth.triggerAttackRelease("A5", noteDuration, now);
synth.triggerAttackRelease("A5", noteDuration, now + interval);
synth.triggerAttackRelease("A5", noteDuration, now + (2 * interval));
```

---

## 💡 建議設定

### 溫和提醒 (適合辦公室)
- 震動: 2次
- 聲音: 1次
- 音量: 較小

### 標準提醒 (目前設定)
- 震動: 5次
- 聲音: 3次
- 音量: 中等

### 強力提醒 (適合嘈雜環境)
- 震動: 10次
- 聲音: 5次
- 音量: 較大

### 超強提醒 (不容錯過)
- 震動: 持續2秒 `[2000]`
- 聲音: 10次
- 音調: 高音 (C6, E6, G6)

---

## 📝 修改步驟

1. 用文字編輯器打開 `index.html`
2. 找到第 665 行 (震動設定)
3. 找到第 678-694 行 (聲音設定)
4. 根據上面的說明修改數值
5. 儲存檔案
6. 重新整理瀏覽器頁面

---

## ⚠️ 注意事項

1. **震動功能**: 只在支援震動的裝置上有效 (主要是手機)
2. **音量控制**: 聲音音量取決於裝置的系統音量
3. **測試建議**: 修改後建議先測試,避免提醒過於擾人
4. **瀏覽器限制**: 某些瀏覽器可能限制震動或聲音的播放

---

## 🎯 快速參考

| 設定 | 位置 | 目前值 |
|------|------|--------|
| 震動次數 | 第 665 行 | 5次 |
| 聲音次數 | 第 678-694 行 | 3次 |
| 震動時長 | 第 665 行 | 400ms |
| 震動間隔 | 第 665 行 | 200ms |
| 聲音間隔 | 第 682, 687, 692 行 | 0.8秒 |
| 音符 | 第 679-693 行 | C5, E5, G5 |

---

如有任何問題,請隨時詢問! 🙂
